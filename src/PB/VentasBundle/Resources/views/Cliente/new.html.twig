{% extends '::layout.html.twig' %}
{% block title "Nuevo Cliente" %}

{% form_theme form 'PBInicioBundle:Form:fields.html.twig' %}

{% block content %}
<div class="title title-spacing ventas">
					<h2>Nuevo Cliente</h2>
</div>  
{% include 'PBVentasBundle:Cliente:form.html.twig' with { 'form' : form } %}
    <div class="float-left volver">
        <a href="{{ path('cliente') }}">
          <<  {{ 'views.recordactions.backtothelist'|trans({}, 'JordiLlonchCrudGeneratorBundle') }}
        </a>
    </div>
{%  endblock %}
