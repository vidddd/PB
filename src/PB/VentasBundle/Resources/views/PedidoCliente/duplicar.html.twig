{% extends '::layout.html.twig' %}

{% block title "Duplicar Pedido" %}
{% form_theme form 'PBInicioBundle:Form:fields.html.twig' %}
{% block width100 "widthtotal" %}{% block displaysidebar "display:none;" %}
{% block content %}
<div class="title title-spacing ventas"><h2>Duplicar Pedido Cliente</h2></div>

	<div id="pedido-form">
		<form action="{{ path('pedidocliente_duplicar_guardar', { 'id': id }) }}" method="post" {{ form_enctype(form) }}>
		{% include 'PBVentasBundle:PedidoCliente:form.html.twig' with { 'form' : form } %}
		</div>
			<div style="display:none;">
				{{ form_errors(form) }} 
				{{ form_rest(form) }}
			</div>
		{{ form_widget(form._token) }}</form>
		    </div>
    
    </div>
    
	<div class="clearfix"></div>
    <div class="float-left volver">
        <a href="{{ path('pedidocliente') }}">
          <<  {{ 'views.recordactions.backtothelist'|trans({}, 'JordiLlonchCrudGeneratorBundle') }}
        </a>
    </div>
    <div class="clearfix"></div><br />
		{% include 'PBVentasBundle:Cliente:js_get_cliente.html.twig' %} 

<script>
getNombrecliente();
        	</script>
{% endblock %}